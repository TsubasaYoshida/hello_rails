= simple_form_for @blog do |f|
  = f.input :title, label: "タイトル", hint: "ブログのタイトルを入力してください"
  = f.input :scope
  = f.input :image
  = f.button :submit

  -# 以下simple_formのinputカスタマイズ実験用
  -#= f.input :title, collection: 1..5, as: :radio_buttons
  -#= f.input :title, collection: 1..5
  -#= f.input :title, label_html: { style: 'color:red;' }, input_html: { style: 'border-radius:5px;' }